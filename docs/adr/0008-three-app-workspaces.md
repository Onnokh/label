# Three App Workspaces

Label will structure the monorepo around three runnable app workspaces: `apps/api`, `apps/web`, and `apps/ios`. This keeps the backend, web companion, and native iOS app explicit without introducing an extra shared core package before one is needed.
