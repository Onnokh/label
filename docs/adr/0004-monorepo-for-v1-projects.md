# Monorepo for V1 Projects

Label will use a monorepo for the native iOS app, backend API, web companion, shared contract, documentation, and ADRs. The product has several coordinated moving parts, and keeping them together reduces early coordination overhead while the domain model and API contract are still changing quickly.
