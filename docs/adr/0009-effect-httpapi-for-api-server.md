# Effect HttpApi for API Server

Label will use Effect HttpApi as the HTTP layer for `apps/api`. This keeps route definitions, request and response schemas, implementation typing, and OpenAPI generation in the Effect ecosystem instead of splitting the backend between Effect services and a separate web framework.
